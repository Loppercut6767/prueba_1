# Proyecto YOLO (clasificación de morfología de cacao)
#
# IMPORTANTE: usa un entorno virtual SEPARADO del de modelo_actual. YOLO/torch y
# TensorFlow pueden chocar en la versión de numpy; aislarlos evita problemas.
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

ultralytics>=8.3        # incluye el modelo YOLO y trae torch/torchvision
scikit-learn>=1.3       # métricas (accuracy, F1, matriz de confusión)
matplotlib>=3.7
